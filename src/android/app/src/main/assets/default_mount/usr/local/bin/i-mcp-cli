#!/bin/sh
# Pre-rebrand name for orca-mcp-cli, kept because it is baked into existing chat
# transcripts the model reads back as context and into user scripts written
# inside the guest. Forwards verbatim; delete once those stop appearing.
exec /usr/local/bin/orca-mcp-cli "$@"
